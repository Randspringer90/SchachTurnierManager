# Handoff-Archiv

Historische, versionsbezogene Übergabedokumente (`HANDOFF_x_y_z.md`) aus der Patch-basierten Entwicklungsphase.

- Diese Dateien werden nicht mehr gepflegt; Pfadangaben darin (z. B. `scripts/After-Apply-*.ps1` oder `docs/HANDOFF_*.md`) beziehen sich auf den damaligen Repository-Stand. Die After-Apply-Skripte liegen heute unter `scripts/archive/after-apply/`.
- Die enthaltenen Befehle nicht mehr ausführen; aktuelle Abläufe stehen in `docs/planning/PROJECT_ORCHESTRATION.md`.
- Der gesamte Ordner wird beim Open-Source-Clean-Snapshot (`scripts/New-OpenSourceSnapshot.ps1`) ausgeschlossen.
